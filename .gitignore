# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
.env/

# ── Generated data & corpora (large, reproduce with scripts/) ─────────────────
data/foundational_corpus.txt
data/instruction_corpus.txt
# Keep data/tokenizer/ tracked — it's small and required for all downstream steps

# ── Model checkpoints (large binary files, >50 MB each) ──────────────────────
models/
*.pt

# ── Experiment outputs: exclude checkpoints, keep results/plots ───────────────
experiments/run_01/*/checkpoint_step_*.pt
experiments/run_01/*/final_model.pt

# ── OS / editor artefacts ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── Claude Code local settings ────────────────────────────────────────────────
.claude/

# ── Presentation build output (generated by presentation/build_presentation.py)
presentation/final_project_presentation.pptx
